<nav class="navbar navbar-expand-lg navbar-light bg-light mb-5">
  <a class="navbar-brand" href="/admin">管理者後台</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <% ["post"].each do |singular_name| %>
      <li class='nav-item <%= 'active' if controller_name == singular_name.pluralize %>'>
        <a class="nav-link py-sm-2 py-3" href="/admin/<%= singular_name.pluralize %>"><%= I18n.t("activerecord.models.#{singular_name}") %></a>
      </li>
      <% end %>
    </ul>
    <form class="form-row align-items-center my-2 my-lg-0">
      <div class="col-auto">
        <input class="form-control mr-sm-2" type="text" placeholder="translation missing: zh-TW.text.search_placeholder" name="s">
      </div>
      <div class="col-auto">
        <button class="btn btn-secondary my-2 my-sm-0" type="submit">translation missing: zh-TW.text.search</button>
      </div>
    </form>
    <% if user_signed_in? %>
      <ul class="navbar-nav ml-sm-5 ml-0">
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            管理者
          </a>
          <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
            <a class="dropdown-item" href="#">個人資料</a>
            <div class="dropdown-divider"></div>
            <%= link_to "登出", admin_logout_path, class: "dropdown-item", method: :delete %>
          </div>
        </li>
        <li class="nav-item active">
        </li>
      </ul>
    <% end %>
  </div>
</nav>